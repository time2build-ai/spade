.venv/
__pycache__/
*.pyc
.pytest_cache/
.superpowers/
.claude/
.agent-comms/

# macOS
.DS_Store

# Large reference mockup (kept locally, not in VCS)
docs/Spade (standalone).html

# Local-only scratch guides (kept locally, not in VCS)
*.local.html

# Testing-guide PDF (kept locally, not in VCS)
Spade-MVP-Testing-Guide.pdf

# Presentation / personal guides — kept locally, not in VCS.
# WORKSHOP.html is the Spanish thesis-demo narration script; USER-GUIDE.html is
# kept local alongside it. (The demo README stays tracked.)
docs/WORKSHOP.html
docs/USER-GUIDE.html
